# СЭС 50 МВт «Толеби» — инвестиционный дашборд

Статичное веб-приложение (без сборки), визуализирующее инвестиционный проект строительства солнечной электростанции 50 МВт в Толеби районе Туркестанской области, Казахстан — на основе [PRD.md](PRD.md).

## Структура

Многостраничный сайт (каждый раздел — отдельный HTML-файл, дизайн пока временный/базовый):

```
index.html       — Обзор (описание, цели, объём работ, стороны)
finance.html      — Финансы (KPI, операционные показатели, чувствительность, CAPEX/OPEX)
production.html   — Производство (площадка, техконфигурация, выработка по годам)
funding.html      — Финансирование (структура капитала, кредиторы, требования МФИ)
roadmap.html      — Дорожная карта (фазы 0–4, следующие шаги)
risks.html        — Риски (риски/меры, KPI успеха, допущения и ограничения)
css/style.css     — общие стили (тёмная тема)
js/main.js        — графики (Chart.js через CDN), подключается на страницах с графиками
PRD.md            — исходный документ требований
```

## Запуск локально

Просто откройте `index.html` в браузере, либо через локальный сервер:

```bash
npx serve .
```

## Деплой на GitHub Pages

1. Запушьте репозиторий на GitHub.
2. В настройках репозитория → **Settings → Pages** → Source: **Deploy from a branch**, branch `main`, папка `/ (root)`.
3. Сайт будет доступен по адресу `https://<username>.github.io/<repo>/`.

Либо используйте workflow в `.github/workflows/pages.yml` (автоматический деплой через GitHub Actions при каждом push в `main`).
